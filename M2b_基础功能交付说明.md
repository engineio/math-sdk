# 《三国五行传》M2b 基础功能交付说明

| 项目项 | 内容 |
|---|---|
| 数学版本 | `m2b.0.0` |
| 日期 | 2026-07-30 |
| 目标 | 在 M2a 自然基线上实现完整基础功能层 |
| 基础游戏 RTP | 100,000 局为 67.6822% |
| 状态 | M2b 工作基线锁定，可进入 M2c 免费旋转 |

## 1. 本次结论

M2b 已实现：

- 2×2 超大符号和簇群 ×2；
- 初始无赢局武魂救援；
- 将魂返场；
- 木、火、金、水、土五项功能队列；
- 按实际功能数应用 ×2、×3、×5、×10 局末倍数；
- 功能、分项 RTP 和触发事件输出；
- 100,000 局 Stake SDK 验证与 RGS 哈希校验。

100,000 局结果：

| 指标 | 结果 |
|---|---:|
| 基础游戏 RTP | 67.6822% |
| 命中率 | 34.0070% |
| 命中局平均赢分 | 1.9902× |
| 标准差 | 10.5319× |
| 最大观察赢分 | 1,974× |
| 双 Wild 入口 | 0.4760%，约 1/210 |
| Books/LUT | 100,000 条 |
| RGS payout hash | 通过 |
| RGS SHA-256 | 通过 |

基础游戏目标为约 68%，当前差异为 -0.3178 个百分点。考虑 M2b 已出现明显重尾，最终偏差应在免费旋转接入后通过完整模式统一调优，不建议继续对基础功能做小样本过拟合。

## 2. 关键数学决策

### 2.1 奇策阈值改为 3/4/5/6

初版测试采用 1/2/3/4 步并允许功能连锁继续充能，1,000 局基础 RTP 达到 8,936.7%。原因是功能生成的赢奖会继续解锁下一功能，形成自激队列，快速到达土系与 ×10。

M2b 锁定：

| 累计有效赢奖步骤 | 解锁 |
|---:|---|
| 3 | 木·箭雨掠阵 |
| 4 | 火·星火斜阵 |
| 5 | 金·金戈肃清 |
| 6 | 水·水镜移星 + 土·武魂降世 |

自然、救援和返场产生的赢奖步骤可充能。功能执行后产生的新连锁继续正常派彩，但不再反向推动本局计量条。

这个规则同时满足：

- 防止功能自激循环；
- 五功能与 ×10 保持低频高波动；
- 功能事件可在有限步数内结束；
- 基础 RTP 可控制在约 68%。

### 2.2 W2 只进入 4 条 reel

救援、返场和五行功能会生成大量 W1。若 W2 仍存在于全部 7 条 reel，双 Wild 入口在 100,000 局中约为 1/179.5，贴近并略超产品上限。

M2b 保留所有 reel 上的 W1，但只在前 4 条 reel 放置 W2；其余 3 个 W2 停点替换为 H1。最终双 Wild 入口约 1/210。

## 3. 功能实现

### 3.1 2×2 超大符号

- 每个稳定盘面识别不重叠的 2×2 同普通符号区域；
- 合并概率为 20%；
- 若支付簇群完整包含超大符号四格，该簇群统一 ×2；
- 一个簇群包含多个超大符号时仍只应用一次 ×2；
- 事件输出符号、四格坐标和倍率；
- 不修改普通赔付表。

M2b 将超大符号实现为“稳定盘面逻辑覆盖层”：每次补位完成后重新识别。如果超大符号没有支付而盘面因其他簇群继续掉落，旧覆盖层失效，新稳定盘面重新判定。客户端不得跨稳定盘面自行保留旧覆盖层。

### 3.2 随机武魂救援

- 只在初始稳定盘面无赢奖时判定；
- 条件触发概率为 4.4%；
- 生成 1–8 个 W1；
- 数量权重为 `35/25/18/10/6/3/2/1`；
- 不覆盖已有 Wild；
- 生成后重新执行双 Wild 检查和簇群计奖。

### 3.3 将魂返场

- 每个付费局以 27% 概率标记一种当前存在的低分符号；
- 被标记符号形成支付簇群时，每个支付区域最多留下 2 个 W1；
- 优先使用原支付区域坐标；
- 补位后若原坐标已为 Wild，使用其他普通合法格回退；
- 返场 Wild 可触发下一次簇群和双 Wild 入口。

### 3.4 木·箭雨掠阵

- 按 `35/25/18/10/6/3/2/1` 权重选择 1–8 个普通格转为 W1；
- 每个新 W1 最多选择一个四方向相邻普通格清除；
- 重复相邻格只清除一次；
- 清除后执行掉落和补位。

### 3.5 火·星火斜阵

- 中央逻辑格 `(3,3)` 设置为 W1；
- 随机选择一个普通付费符号；
- 两条完整对角线上的非 Wild 格转为该符号；
- 不覆盖已有 W1/W2。

### 3.6 金·金戈肃清

- 清除当前盘面的所有 L1–L5；
- 统一掉落和顶部补位；
- 不清除高分武将或 Wild。

### 3.7 水·水镜移星

- 从当前存在的 L1–L5 中随机选择源符号；
- 将全部源符号转为另一种随机普通付费符号；
- 若当前无低分符号，按合法普通符号执行确定性回退。

### 3.8 土·武魂降世

按三个阶段执行：

1. 随机放置一个 3×3 W1 区域并完成全部连锁；
2. 清理仍存活的上一阶段 W1，放置两个不重叠 2×2 W1 区域并完成连锁；
3. 清理仍存活的上一阶段 W1，随机放置九个 1×1 W1 并完成连锁。

每个阶段输出覆盖位置、被覆盖符号、清理位置和完整结果盘面。

## 4. 局末倍数

| 实际执行功能数 | 倍数 |
|---:|---:|
| 0–1 | ×1 |
| 2 | ×2 |
| 3 | ×3 |
| 4 | ×5 |
| 5 | ×10 |

实现顺序：

1. 累计自然、救援、返场和功能产生的原始赢分；
2. 应用局部超大符号 ×2；
3. 所有队列和连锁结束后，按不同功能数计算总倍数；
4. 最后应用 50,000× 封顶。

## 5. 100,000 局 RTP 分项

| 分项 | RTP 贡献 |
|---|---:|
| 自然簇群 | 47.1256% |
| 2×2 超大符号额外贡献 | 2.9876% |
| 武魂救援 | 2.4286% |
| 将魂返场 | 2.5400% |
| 木 | 1.0926% |
| 火 | 0.3964% |
| 金 | 0.2376% |
| 水 | 0.0050% |
| 土 | 0.6946% |
| 局末倍数额外贡献 | 10.1742% |
| 合计 | 67.6822% |

功能执行分布：

| 实际功能数 | 局数 | 频率 |
|---:|---:|---:|
| 0 | 99,021 | 99.021% |
| 1 | 803 | 0.803% |
| 2 | 134 | 0.134% |
| 3 | 34 | 0.034% |
| 4 | 0 | 0% |
| 5 | 8 | 0.008% |

第四阈值会同时加入水和土，因此普通模式自然路径从 3 个功能直接进入 5 个功能。4 功能 ×5 主要为未来功能购买和其他模式保留。

## 6. 事件合同

M2b 新增事件：

| 事件 | 用途 |
|---|---|
| `megaSymbolsDetected` | 2×2 合并位置和倍率 |
| `returnSymbolMarked` | 本局返场目标 |
| `returnWildsApplied` | 返场 Wild 坐标和结果盘面 |
| `rescueApplied` | 救援 Wild 坐标和结果盘面 |
| `featureQueued` | 功能、充能值和队列 |
| `featureApplied` | 功能阶段、选择结果和完整盘面 |
| `roundMultiplierApplied` | 功能数、原始赢分、额外赢分和总倍数 |
| `rtpComponentSummary` | 本局分项赢分、充能和执行功能 |

金额字段统一使用百分之一投注倍数整数。客户端只播放事件，不重新随机选择位置或符号。

## 7. 测试与保护

- 10 项配置和功能测试通过；
- 固定种子回合可复现；
- Wild 单一归属测试继续通过；
- M2a 历史 reel 已归档并保留原 SHA-256；
- M2b 当前 reel SHA-256 为 `ec90278c94ac76366bbe7000986cba3b1a36aaf238d54589de5128dbcba3426c`；
- 每局最多 100 个赢奖步骤；
- 每局最多 1,000 个 Book Event；
- 100,000 条 Books/LUT payout hash 和 SHA-256 通过。

当前上游 SDK 的多批次输出存在验证侧车条目错误。候选验证固定使用单线程、单批次；启用大规模多线程前需另行修复合并器。

## 8. 已知限制

- M2b 不是完整 96% 游戏，尚缺约 28% 免费旋转预算；
- 100,000 局已显示标准差 10.53×，不足以稳定估计极低频尾部；
- 2×2 超大符号是稳定盘面覆盖层，不是跨掉落持久化刚体；
- 土系只有 8 次样本，1,974× 不是最大可能赢分；
- 双 Wild 当前只记录候选事件，尚未进入免费旋转；
- 正式数学锁定仍需普通模式至少 1 亿局。

## 9. 下一阶段

M2c：

1. 将双 Wild 候选升级为正式免费旋转触发；
2. 实现初始 5 次免费旋转；
3. 实现 W1 从 1×1 至 7×7 成长；
4. 实现等级奖励旋转和 ×5/×7/×10；
5. 关闭免费旋转中的超大符号、救援、返场和奇策队列；
6. 调整免费模式专用 W2 权重；
7. 将完整普通模式校准至 96.0%；
8. 增加 100× Bonus Buy 并独立验证。

## 10. 交付入口

- [M2b 验证快照](games/0_0_sanguo_wuxing/validation/m2b_100k_summary.json)
- [M2b 分析器](games/0_0_sanguo_wuxing/analyze_m2b.py)
- [M2b 状态机](games/0_0_sanguo_wuxing/gamestate.py)
- [基础功能实现](games/0_0_sanguo_wuxing/game_executables.py)
- [功能事件](games/0_0_sanguo_wuxing/game_events.py)
