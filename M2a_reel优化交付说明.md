# 《三国五行传》M2a Reel Strip 优化交付说明

| 项目项 | 内容 |
|---|---|
| 数学版本 | `m2a.0.0` |
| 日期 | 2026-07-30 |
| 目标 | 将 Stake SDK 自然 RTP 从 59.2540% 调回约 48.5% |
| 结果 | 100,000 局自然 RTP 48.3824% |
| 状态 | M2a 基线锁定，可进入基础功能开发 |

## 1. 结论

M2a 在不修改用户可见赔付表的前提下，通过扁平化符号计数和选择确定性 reel 排列，将自然簇群 RTP 从 M1 的 59.2540% 降至 48.3824%。

同时满足：

- 自然命中率 33.1610%，位于 30%–45% 目标区间；
- 双 Wild 同现约 1/244，位于 1/180–1/260 目标区间；
- 100,000 条 Books/LUT payout hash 一致；
- 发布文件 SHA-256 校验通过；
- 赔付表、Wild 归属算法与 50,000× 封顶均未改变。

## 2. M1 与 M2a 对比

| 指标 | M1 | M2a | 变化 |
|---|---:|---:|---:|
| 自然 RTP | 59.2540% | 48.3824% | -10.8716 个百分点 |
| 自然命中率 | 37.2760% | 33.1610% | -4.1150 个百分点 |
| 命中局平均赢分 | 1.5896× | 1.4590× | -0.1306× |
| 标准差 | 1.1561× | 1.0116× | -0.1445× |
| 最大观察自然赢分 | 17.0× | 20.0× | 样本波动 |
| 双 Wild 频率 | 约 1/230 | 约 1/244 | 均在目标内 |

M2a 的目的不是降低完整游戏波动率，而是把自然盘面从过高预算中释放出来。后续五行队列、局末倍数和免费旋转将重新建立长尾。

## 3. Reel 配置

每条 reel 长 719，包含 717 个付费符号和 2 个 Wild。

| 符号 | M1 每 reel 数量 | M2a 每 reel 数量 |
|---|---:|---:|
| H1 | 17 | 20 |
| H2 | 25 | 28 |
| H3 | 33 | 36 |
| H4 | 42 | 45 |
| H5 | 50 | 53 |
| L1 | 75 | 76 |
| L2 | 92 | 91 |
| L3 | 108 | 106 |
| L4 | 125 | 120 |
| L5 | 150 | 142 |
| W1 | 1 | 1 |
| W2 | 1 | 1 |

生成参数：

- 概率扁平化指数：`0.90`；
- reel 排列种子：`906502183`；
- reel SHA-256：`e0b86d0082b7fcc59aeab49ac96873f917568003529750f49cf26694862f41d2`；
- 生成器：`generate_reels.py`；
- 搜索器：`tune_reels.py`。

排列种子属于数学配置的一部分。相同符号计数但不同排列会改变纵向连通、补位路径和双 Wild 同现率，不允许上线前随意重新洗牌。

## 4. 搜索方法

搜索过程使用真实 `GameState`、真实 reel window、真实 tumble 和项目 Wild 归属算法，不使用独立抽样近似：

1. 对原始权重应用 0.84–0.90 的指数扁平化；
2. 使用最大余数法转换为每 reel 717 个付费停点；
3. 为各候选生成 7 条确定性排列；
4. 先跑每候选 5,000 局宽搜索；
5. 对 0.88–0.90 区域跑每候选 20,000 局窄搜索；
6. 选定指数 0.90、种子 906502183；
7. 使用正式 SDK 管线跑 100,000 局；
8. 校验 Books、LUT、force record 和发布哈希。

候选评分同时约束：

- 自然 RTP 接近 48.5%；
- 命中率位于 30%–45%；
- 双 Wild 频率位于 1/180–1/260。

## 5. 100,000 局锁定结果

| 指标 | 结果 |
|---|---:|
| 自然 RTP | 48.3824% |
| 自然命中率 | 33.1610% |
| 零赢率 | 66.8390% |
| 命中局平均赢分 | 1.4590× |
| 标准差 | 1.0116× |
| 最大观察自然赢分 | 20.0× |
| 双 Wild 候选 | 410 次 |
| 双 Wild 频率 | 0.4100%，约 1/244 |
| RGS Books/LUT 条目 | 100,000 |
| Payout hash | 通过 |
| SHA-256 | 通过 |

完整结果快照位于：

- `games/0_0_sanguo_wuxing/validation/m2a_100k_summary.json`
- `games/0_0_sanguo_wuxing/reels/BR0.metadata.json`

## 6. RTP 后续预算

以 M2a 结果为基础，完整 96.0% 尚有约 47.6176 个百分点可分配。继续采用下列工作预算：

| 模块 | 预算 |
|---|---:|
| 自然簇群 | 约 48.4% |
| 2×2 超大符号 | 约 3.0% |
| 随机武魂救援 | 约 2.5% |
| 将魂返场 | 约 2.5% |
| 五行队列与局末倍数 | 约 11.6% |
| 免费旋转和成长 | 约 28.0% |
| 合计 | 96.0% |

这些仍是调优预算，不是最终认证分项。任何功能的实际贡献偏移都必须通过完整模式重新平衡。

## 7. 回归要求

后续任何数学改动必须检查：

- `BR0.csv` 哈希是否有意变化；
- 自然 RTP 是否仍在 48.0%–49.0% 工作区间；
- 自然命中率是否仍在 30%–45%；
- 双 Wild 频率是否仍在 1/180–1/260；
- Wild 单一归属测试是否通过；
- 100,000 局 Books/LUT 哈希是否一致。

接入功能后，“自然 RTP”必须作为独立统计项保留，不得只报告完整 RTP。

## 8. 下一阶段

M2b 建议实现基础功能，但先不接免费旋转：

1. 2×2 超大符号；
2. 无赢局随机武魂救援；
3. 将魂返场；
4. 五行奇策队列；
5. ×2、×3、×5、×10 局末倍数；
6. 把完整基础游戏调至约 68%。

完成 M2b 后，再进入 M2c 免费旋转与 100× Bonus Buy。

## 9. 交付入口

- [M2a 验证快照](games/0_0_sanguo_wuxing/validation/m2a_100k_summary.json)
- [Reel 元数据](games/0_0_sanguo_wuxing/reels/BR0.metadata.json)
- [Reel 生成器](games/0_0_sanguo_wuxing/generate_reels.py)
- [Reel 搜索器](games/0_0_sanguo_wuxing/tune_reels.py)
- [M2a 游戏说明](games/0_0_sanguo_wuxing/README.md)
